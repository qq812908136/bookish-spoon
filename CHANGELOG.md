# 更新日志（CHANGELOG）

> **维护约定**
> 1. 按版本倒序记录，**只写对用户可见的变更**（功能、修复、结构），内部过程记录放 `docs/迭代记录/`。
> 2. 每个版本标注日期与对应交付包；已发行的版本**不再改写**，发现遗漏在下个版本补记。
> 3. 版本格式参照 [语义化版本](https://semver.org/lang/zh-CN/)：主版本.次版本.修订号。
> 4. 上线前必读 `docs/上线前待办.md`（含 P0/P1 安全整改项）。

---

## [未发布] V4（进行中）

仓库结构重构 + 工程化补齐 + 安全整改 + 邮件通知功能。**原有功能与页面交互保持不变**（安全整改只新增校验，邮件为纯增量），全量测试 **212 项**全部通过。

### 功能

- **新增：邮件通知通道（V4 主体功能）** —— 在站内信之外增加一路邮件触达。
  - **触发场景**：任务分配 / 改派通知、逾期提醒、即将到期提醒、长期待激活提醒、管理员每日 09:00 汇总日报、管理员与创建人在任务里的手动提醒。
  - **默认关闭**：`MAIL_ENABLED` 默认 `false`。**不配置 = 一封不发**，行为与 V3 完全一致，已发行的离线程序不受影响。
  - **配置页**：管理员顶部导航新增「邮件」入口，一页看完状态概览、SMTP 配置、测试邮件、未填邮箱名单、失败清单与发送记录（筛选 + 分页）。
  - **测试邮件不走队列**，点击即时连接 SMTP —— 不必等第二天 09:00 才知道配得对不对。
  - **防打扰**：同一负责人的多个逾期任务**合并成一封**；管理员只收日报，不收他人任务的零散邮件；每轮限量发送（默认 20 封 / 5 分钟）。同等规模下由「500+ 封/天」降至约 30 封/天。
  - **失败可查可重发**：失败件在页面直接列出原因并支持一键重发；按 5 / 15 / 30 分钟指数退避重试（最多 3 次）。
  - **熔断保护**：认证失败（授权码错）**立即熔断并需人工恢复**，避免一晚上几千次失败登录导致发件邮箱被封号；其他故障连续失败 10 封后暂停 60 分钟并自动试探恢复。
  - **个人订阅等级**：每人可在「个人设置 → 邮件通知」选收多少（不接收 / 仅逾期 / 逾期+即将到期 / 全部），并查看「发给我的」邮件记录。
  - **隐私**：SMTP 授权码加密入库、不出现在页面与日志；用户邮箱仅本人与管理员可见，用户列表页不显示邮箱列；支持邮件标题隐去任务标题（只留编号）。
  - **配置优先级**：环境变量 > `.env` > 页面保存值 > 代码默认值；被环境变量占住的项在页面上标注「已被环境变量锁定」，避免「保存了没变化」的困惑。
  - 📖 管理员配置步骤、常见邮箱 SMTP 参数与排障手册见 **`docs/督办系统-邮件功能配置指南.md`**。

#### 技术说明（邮件功能）

- 新增模块：`crypto_util.py`（PBKDF2 + shake_256 XOR，零新增依赖）、`mail_constants.py`、`mail_service.py`、`mail_templates.py`、`mail_dispatcher.py`、`routes/mail_routes.py`。
- 数据库幂等迁移 `_migrate_v3()`：`users` 增加 `email` / `mail_notify_level` 两列，新建 `email_queue` / `email_log` 两表。**老库直接升级即可，不需要重新初始化。**
- 邮件先落库进队列，由**已有的**每 5 分钟逾期扫描循环顺带发送，**不新增线程**。
- 刻意不改 `models.create_message()` —— 否则站内信的所有类别（状态变更、批示等）都会变成邮件，把高价值低频消息淹没。

### 结构

- **源码分层**：仓库根 → `src/`（应用源码）+ `tests/` + `scripts/` + `docs/` + `data/` + `deliverables/` + `_archive/`
  - `src/config.py` 改为按自身位置推导 `BASE_DIR`（向上退一级 = 项目根）与 `BUNDLE_DIR`（= `src/`），打包后仍退化为 `exe 所在目录` 与 `sys._MEIPASS`，**数据库位置不变**。
  - 测试入口由硬编码绝对路径改为推导（`tests/test_suite.py`）。
- **构建产物隔离**：`build/`、`dist/`、发行版 zip、`env_info.txt`、v1/v2 交付包（约 117 MB）移入 `_archive/` 并加入 `.gitignore`。版本库体积由 126 MB 降至 7.5 MB。
- **交付包生成脚本化**：`scripts/build_delivery_package.py`
  - 路径全部基于 `__file__` 推导，不再依赖当前工作目录。
  - 版本白名单闸门 `REBUILDABLE`：v1/v2/v3 **一律拒绝重建**，防误删已发行包。
  - `05_离线程序` 从 v3 只读复制时**保留内置演示数据库**（9 用户 / 45 任务）——此前因 `.db` 一刀切过滤被误删，会导致离线包启动后进入初始化向导而非直接演示。
  - 自动同步最新打包产物到离线 exe（只替换 `exe` 与 `_internal`，保留 `data/` 与启动 bat）。
  - 自动去掉「-最新」等易腐命名。

### 工程化补齐

- **配置支持环境变量**：新增 `.env.example`。优先级 `系统环境变量 > .env 文件 > 代码默认值`，**未配置时行为与之前完全一致**。
  - 可覆盖项：`SECRET_KEY`、`HOST`、`PORT`、`DEBUG`、`SERVER`、会话 Cookie 三件套（`SESSION_COOKIE_HTTPONLY` / `SAMESITE` / `SECURE`）、CSRF 两项（`CSRF_ENABLED` / `CSRF_TOKEN_MAX_AGE`）、登录安全四项（`LOGIN_MAX_ATTEMPTS` / `LOGIN_LOCKOUT_MINUTES` / `PASSWORD_MIN_LENGTH` / `PASSWORD_REQUIRE_STRENGTH`）、反向代理两项（`BEHIND_PROXY` / `BEHIND_PROXY_TRUSTED_HOPS`）、`LOG_LEVEL`、`LOG_MAX_BYTES`、`LOG_BACKUP_COUNT`。
  - 不引入 `python-dotenv` 依赖（避免重打包成本），改用内置 20 行极简 `.env` 解析。
  - `.env` 已加入 `.gitignore`，防止密钥入库。
- **文件日志**：新增 `logs/supervision.log`，按大小轮转（默认单文件 2 MB、保留 5 份）。
  - 此前仅有控制台输出，程序关闭后无从追查。
  - 日志目录落在程序运行目录（开发时项目根，打包后 exe 同级）。
  - 目录不可写时**静默降级为仅控制台**，不会因日志配置失败导致启动失败。
- **新增 `CHANGELOG.md`**（本文件）。
- **新增 CI 配置** `.github/workflows/ci.yml`（需推送到远端仓库后才生效）。

### 修复

- **任务列表：勾选任务不再把下方布局顶下去**（改动待办清单 #060）。
  - 现象：批量操作工具栏（「已选 N 条 / 选择操作 / 应用」）独占一行，勾选第一条任务时整条出现，表格和分页整体下移；取消勾选又弹回去。
  - 现在：工具栏挪到页头标题「任务列表」右侧，在标题行内出现/消失，下方布局不动。
- **静态资源版本号集中在 `config.STATIC_VERSION`**（修「改了样式不生效」）。
  - 现象：改过 CSS/JS 之后，**登录页和初始化向导页仍加载旧样式**，需要用户手动清缓存。
  - 原因：静态资源的缓存串（`main.css?v=20260830af`）原本写死在每个模板里。此前升级时只改了 `base.html`，而 `login.html` / `setup.html` 是独立页面（不继承 `base.html`），版本号没跟上，浏览器于是继续用缓存里的旧文件。
  - 现在：版本号只有一个来源（`config.STATIC_VERSION`），由全局上下文注入所有模板，**改样式只改这一处即可全局生效**。
  - 新增 2 项测试守住：模板里不许再出现硬编码版本号；登录页 / 初始化向导页 / 已登录页面必须拿到同一个版本号。

### 安全

- **修复 DEF-001（P0）：会话签名密钥不再硬编码。**
  - 此前密钥是写死在源码中的固定字符串，会随 exe/源码分发给所有部署方，任何拿到程序的人都能伪造管理员会话 Cookie 免密登录。
  - 现在密钥**不再有默认值**：首次启动用 `secrets.token_hex(32)` 生成并写入 `data/secret.key`，之后一直复用；也支持用 `SECRET_KEY` 环境变量显式指定。
  - **每个部署实例的密钥都不相同**，源码与打包物中不含任何可用密钥。
  - `data/secret.key` 已加入 `.gitignore` 并在构建脚本中排除，**不会随交付包分发**（否则所有实例共用同一把密钥，修复等于白做）。
  - 部署目录只读无法落盘时，退回内存随机值继续运行（重启后会话失效），不阻断启动。
  - ⚠️ **升级后所有用户会被强制重新登录一次**，属预期行为。

- **修复 DEF-004（代码侧）：默认不再对局域网开放。**
  - 此前 `HOST` 默认为 `0.0.0.0`，每台部署机器都会默认对整个局域网开放，而使用者往往并不知情。
  - 现在默认监听 **`127.0.0.1`**，只有这台电脑能打开。
  - 局域网开放改为**显式动作**：新增 `局域网启动.bat`（启动时设 `HOST=0.0.0.0`）。原先的 `开启局域网访问.bat` 只管防火墙，两个脚本需配合使用，脚本内已互相提示。
  - ⚠️ **升级后同事将无法访问，需要局域网共享时请改用 `局域网启动.bat`。**
- **会话 Cookie 加固**（顺带缓解部分 CSRF 风险）：新增 `HttpOnly` + `SameSite=Lax`，`Secure` 可按 HTTPS 情况开启。
  - `SameSite=Lax` 让浏览器在跨站 POST 时不携带会话 Cookie，可挡住最常见的表单型 CSRF。完整的 token 校验（DEF-002）仍未实现。
- **支持生产级 WSGI 服务器**：新增 `SERVER=waitress`，可切到 Waitress。
  - Waitress 是**可选依赖**，未安装时自动回退 Flask 开发服务器并告警，不会因为缺包起不来。
  - 生产部署的完整步骤（含 HTTPS、备份）见 `docs/生产部署指南.md`。
- **DEF-004 部署侧配件已交付（2026-09-01）**：离线程序(`.exe`) 已将 `waitress` 打包进 `_internal/`，`生产启动.bat` 直接以 WSGI 运行；随附 `生产启动.bat`（一键 Waitress 启动 + 启动前自动备份 `data/`）、`备份数据.bat`（带时间戳备份、计划任务友好）、`Caddyfile.example`（HTTPS 反向代理样例）。HTTPS/域名/证书等上线动作仍按 `docs/生产部署指南.md` 在目标机执行。

- **修复 DEF-005：登录限流 + 弱口令拦截。**
  - **登录失败限流**：按「客户端 IP + 用户名」组合计数，连续失败 5 次后锁定 15 分钟。
    - 锁定期内**即使输入正确密码也会被拒绝**，到期自动解锁，登录成功立即清零计数。
    - 失败时提示「还可尝试 N 次」，被锁时提示剩余等待时间。
    - 两个维度缺一不可：只看 IP 会让共用出口 IP 的同事互相误伤，只看用户名则挡不住遍历账号。
  - **弱口令拦截**：新增密码强度校验，覆盖**全部 4 个设置入口**（初始化向导 / 新建用户 / 重置密码 / 个人改密码）——此前仅初始化向导有一句「至少 6 位」。
    - 规则：长度 ≥ 8、不能全是数字、不能全是字母、不在常见弱口令黑名单内、不能与用户名相同。
    - 刻意**不要求**「大小写 + 特殊符号」这类强组合，内部系统门槛过高只会促使把密码贴在显示器上。
  - ⚠️ **不追溯已有账号**：历史遗留的 6 位弱密码账号**仍可正常登录**，只在下次改密码时才要求达标。
  - 新增配置项（均可在 `.env` 中调整）：`LOGIN_MAX_ATTEMPTS`、`LOGIN_LOCKOUT_MINUTES`、`PASSWORD_MIN_LENGTH`、`PASSWORD_REQUIRE_STRENGTH`。
  - 登录成功/失败均写入日志（含 IP、用户名、失败原因、剩余次数），便于事后追查。
  - 📌 **演示库弱口令已于 2026-09-01 清理**：`admin` 与 8 位负责人口令由 `admin123` / `123456` 改为强口令（`Supv#Admin2026` / `Supv#Owner2026`），种子脚本 `src/seed_demo_data.py` 与初始化向导 `src/app.py` 同步更新，离线包内置演示库一并刷新；限流挡得住爆破、已知弱口令风险已闭环。

- **清理演示库弱口令（2026-09-01）**：将演示数据（`data/supervision.db` 与离线包内置库）中 `admin` / `owner` 角色账号的弱口令替换为强口令，并同步更新 `src/seed_demo_data.py`、`src/app.py` 初始化向导的默认口令与提示文案；不影响任何业务功能与任务数据，全量测试不受影响。
  - 📌 部署在反向代理后需正确传递客户端 IP，否则所有请求会记成代理服务器的 IP，导致一人被锁、全员陪绑（见 `docs/生产部署指南.md`）。

- **修复 DEF-002：全站 CSRF 防护。**
  - 此前所有写操作路由均无令牌校验，已登录用户访问恶意页面时，攻击者可借其身份发起伪造请求（删任务、改状态、建管理员账号等）。
  - 现在所有写请求（POST / PUT / PATCH / DELETE）都要带 CSRF 令牌，**24 条写路由无一例外**；缺失或无效令牌一律拒绝。
  - 令牌可从三处携带：表单字段 `csrf_token`、请求头 `X-CSRF-Token`、JSON body 的 `csrf_token` 键。
  - **21 个 POST 表单**（跨 10 个模板）已自动带上 hidden 字段，**8 处 AJAX 请求**由 `main.js` 统一注入请求头，业务流程无感。
  - 校验放在 `before_request` 全局钩子而非逐路由装饰器 —— **将来新写的 POST 路由默认就是受保护的**，不依赖写代码的人记得加。
  - 登录成功时会轮换会话密钥，登录前签发的令牌作废。
  - 失败响应区分场景：AJAX 返回 JSON（供前端解析提示），普通表单提交返回一个可读的 400 页面（提示刷新后重试）。
  - 新增配置项（均可在 `.env` 中调整）：`CSRF_ENABLED`（默认 true，**生产务必保持开启**）、`CSRF_TOKEN_MAX_AGE`（默认 24 小时）。
  - 与 DEF-004 的 `SameSite=Lax` 构成两层防护，互不依赖。
  - 📌 **页面停留超过 `CSRF_TOKEN_MAX_AGE` 后提交会提示刷新**；若内部系统习惯长期开着页面，可把该值调大。
  - 📌 二次开发新增写操作路由时无需额外动作（全局钩子已覆盖），但**新增表单必须带 `{{ csrf_token }}` hidden 字段**，测试套件的防回归用例会检查。

### 启动脚本

- **5 个 bat 全部重写，改为「运行方式自适应」**：目录里有 `督办系统.exe` 就跑 exe，没有就用 Python 跑源码。
  - 因此仓库根与离线程序**共用同一份脚本**，不必再维护两套。
  - 仓库根成为启动脚本的唯一真相源，构建时自动同步进离线程序目录。
- **修复隐患**：此前每次构建都从 v3 基线重新复制 `05_离线程序/`，导致**离线目录里的 bat 永远停留在 v3，改仓库根的脚本根本不生效**。现已修正。
- 新增 `局域网启动.bat`；新增 `scripts/check_bat_encoding.py`（bat 编码守卫，防止有人用 UTF-8 改坏脚本导致中文乱码）。

### 文档

- 新建 `docs/README.md` 作为文档唯一真相源入口：命名对照表、两份架构文档分工澄清、文档地图、与交付包的映射关系。
- 补齐 4 份缺失设计文档（接口 / 数据库 / 模块详细 / 系统架构设计）。
- 交付包设计文档去掉易腐的「-最新」后缀（v4 起为 `督办系统-架构设计.md`；**v3 已发行，保持原样**）。
- 新增 `docs/项目目录结构说明.md` 作为目录结构唯一真相源。
- 新增 `docs/生产部署指南.md`：三种部署形态（本机 / 局域网 / 生产）的选择、Waitress + HTTPS 反向代理的落地步骤、配置项速查、备份与日志说明、上线前检查清单。
- 新增 `docs/督办系统-邮件功能配置指南.md`：管理员配置 SMTP 的步骤、常见邮箱服务商参数、状态页各项指标的含义、五种故障的排障路径。
- **两份架构文档补齐邮件模块**（此前对 `mail_` 的提及数为 0，设计层完全空白）：
  - `docs/architecture-design.md` 新增**第十三章「邮件通知模块（V4 迭代）」**——数据模型（V3 迁移）、配置三级优先级、凭据加密、7 种邮件类型与 4 级订阅、6 个入队入口与去重键粒度、`scan_and_send` 流程、重试退避、双重熔断、调度挂钩点（含行号）、8 条路由与权限、隐私硬约束、静默降级清单、排障路径。
  - `docs/督办系统-系统架构设计.md` 新增**第 9 章「V4 架构增补（邮件通知）」**——六项选型与权衡：落库队列 vs 直接发送、复用 5 分钟扫描 vs 新增线程、入队阶段合并 vs 发送阶段聚合、标准库自建加密 vs cryptography、双重熔断 vs 统一一套、默认值必须保守的部署约束；含 V3/V4 架构影响对照表。
  - 两章**视角不同不重复**：第十三章给改代码的人看，第 9 章给评审与接手的人看。

---

## [v3.0] 2026-08-31（已发行）

对应交付包 `deliverables/督办系统-交付包-v3/`。Git tag：`v3.0`。

### 功能

- **任务导出改为 Excel**（原 CSV）：`.xlsx` 格式，每列自适应列宽，中文文件名走 RFC 5987 双兜底。
- **品牌图标替换为 SVG**（剪贴板 + 对勾），去除外部依赖，离线环境正常显示。
- **发送消息页关联任务**：改为可按任务名称搜索的下拉选择。

### 修复

- **双种子问题**：`--seed-demo` 原本调用 `app.py` 内嵌的精简种子（5 用户 / 12 任务），与完整扩充脚本（9 用户 / 45 任务）不一致，导致离线包演示数据永远只有 5/12。已改为统一委托 `seed_demo_data.main()`，现为 **9 用户 / 45 任务 / 67 消息**。
- 新建任务页截止日期选中后显示异常。
- 登出提示泄漏到首页；顶栏品牌区改为不可点击。
- 页面切换时来回晃动（预留滚动条轨道 + flash 平滑收起）。

### 质量

- 全量测试 **121 / 121 通过**。
- 输出测试报告（`docs/测试报告/`，含 MD / HTML / 用例清单 / `test_summary.json`）。
- 建立缺陷清单（17 条）与 `docs/上线前待办.md`。
- 交付包五段式齐全：`01_需求文档 / 02_设计文档 / 03_开发代码 / 04_测试 / 05_离线程序`。

### 已知问题（不阻塞演示，上线前必须整改）

| 编号 | 问题 | 级别 |
|---|---|---|
| DEF-001 | `SECRET_KEY` 硬编码，可被伪造管理员会话 | P0 |
| DEF-002 | 全站无 CSRF 防护 | P1 |
| DEF-004 | 监听 `0.0.0.0` 且使用 Flask 开发服务器 | P1 |

> V4 已为 DEF-001 铺路（`SECRET_KEY` 现可通过环境变量或 `.env` 覆盖），但默认值仍是公开字符串，**上线前必须显式配置**。

---

## [v2.0] 2026-08-30

V2 迭代：抽屉交互 + 数据模型扩展 + 视觉体系重做。详见 `docs/iteration-v2-design.md`。

### 功能

- **抽屉交互**（核心）：任务详情抽屉、Owner 抽屉与推送提醒、消息抽屉（替代原独立消息中心页）、闭环矩阵点击数字弹出状态抽屉。采用「整页渲染 + 片段注入」，不引入前端框架。
- **数据模型扩展**：
  - `tasks` 表新增 3 列：`progress_percent`（进度百分比）、`risk_note`（风险说明）、`collaborators`（协作人）。
  - 新增 `evidence`（过程证据）、`blockers`（阻塞记录）两张表。
  - 老库由 `models._migrate_v2()` 启动时**自动无缝迁移**，无需手工处理。
- **仪表盘 V2**：新增统计三函数，支持 `?range=` 时间范围筛选。
- **路由扩展**：26 → 37 个端点（新增抽屉片段路由 ×3、行内编辑、证据/阻塞 CRUD ×5、推送提醒）。
- **视觉体系**：浅色 / 深色双套 CSS 变量、Lucide 图标离线化、响应式断点（≥1200 双栏 / <1200 矩阵独占 / ≤1024 三列 / ≤768 手机端）。
- **术语统一**：全站「行动」→「任务」（任务总数、任务数、任务闭环矩阵）。

### 修复（UI 微调批次 001–059，共 54 项已修复 / 3 项已回退 / 1 项已放弃）

- 闭环矩阵表头改为完整状态名并固定列宽，翻页不再横向晃动。
- 任务列表固定行高（定稿 56 px）与 `table-layout: fixed`，翻页不再变样。
- 分页统一改为 AJAX 原地刷新，根治翻页时滚动条跳顶 / 跳底。
- 焦点列表底部预留呼吸位，hover 阴影不再被裁切。
- 静态资源引用加 `?v=` 版本号，根治浏览器缓存导致的样式不生效。

---

## [v1.0] 2026-08-25（基线）

首个可用版本，确立系统核心模型。仓库于 2026-08-30 才初始化 Git，此前的提交历史不在版本库中。

### 功能

- **角色体系**：管理员（admin）+ 负责人（owner），统一任务池，权限按角色区分。
- **状态机（5 态）**：待启动 → 进行中 → 已逾期 → 已闭环 → 已撤销。
  - 「已逾期」是**状态**而非标签，由系统每 5 分钟自动扫描触发。
  - 已闭环 / 已撤销为终态，管理员可重开。
- **三层预警**：即将到期（默认 3 天）/ 已逾期 / 长期待激活（默认 7 天），阈值可在系统设置中配置。
  - 每日 09:00 扫描即将到期与长期待激活；每 5 分钟扫描逾期。
- **站内信**：消息永久保留，不随任务删除。
- **形态**：Web + H5 响应式，服务端渲染（Jinja2），无前端框架与构建工具。
- **账号体系**：独立账号（未接入 SSO / 企业微信）。
- 路由 26 个，覆盖认证、任务、进度、消息、用户、系统设置六大模块。

### 部署

- 技术栈：Python 3.10+ / Flask 3.x / SQLite / Jinja2。
- 打包：PyInstaller onedir，解压后双击 bat 启动，监听 5000 端口。
- 首次启动进入初始化向导创建管理员（不预置默认账号）。

---

## 版本与交付包对照

| 版本 | 日期 | 交付包 | Git |
|---|---|---|---|
| V1.0 | 2026-08-25 | `deliverables/督办系统-交付包/`（已归档） | — |
| V2.0 | 2026-08-30 | `deliverables/督办系统-交付包-v2/`（已归档） | — |
| V3.0 | 2026-08-31 | `deliverables/督办系统-交付包-v3/` | tag `v3.0` |
| V4 | 进行中 | `deliverables/督办系统-交付包-v4/` | `main` |

# 2026-08-28 督办系统交付打包工作日志

## V2 迭代：阶段 4（测试与交付）完成 —— V2 全部收尾

- **4-A 全量回归（严过关）**：test_suite.py 87→119 项。更新 3 条旧用例断言（/messages→301 跳 /dashboard、错密码/停用账号登录→401）；新增 32 项 V2 用例（8 模块：仪表盘闭环率口径/矩阵聚合/时间筛选、行内编辑权限矩阵、证据阻塞 CRUD、时间线留痕、抽屉路由、remind、暗色模式/记住我/登录字段错误、表单 3 字段）。119/119 通过，主理人独立复跑确认
- QA 顺手修 2 处：①登录页记住用户名不回填（login.html `username|default()` 对空串不回退 → `username or remembered_username or ''`）；②reset_database 补 DROP evidence/blockers（防 ID 撞残留数据）
- docs/test-cases-report.html 完全重写（119 条、V2 分组徽标）
- **4-B 文档同步（许清楚）**：PRD-draft-v1.md 追加第十一章「V2 迭代增补」（V2-01~08、数据模型、交互变更、兼容性、非目标）；architecture-design.md 更新（evidence/blockers schema、ON DELETE CASCADE、V2 迁移、完整路由表、抽屉契约）
- **4-C 打包发行（寇豆码）**：PyInstaller 重打包（exe 2026-08-28 08:36）；deliverables/督办系统-交付包-v2/（五段式沿用 V1）+ 督办系统-交付包-v2.zip（13.2MB）；01 需求规格说明书融入 V2 增补、02 设计文档 4 份同步、04 测试文档 119 项、05 离线程序含演示库（12 任务/5 用户种子）
- 主理人抽查：全部 9 个 bat GBK+CRLF 编码通过；05 演示库 12 任务干净基线；端口/进程无残留
- **V2 迭代（阶段 1 差异分析 → 阶段 2 设计 → 阶段 3 四批次开发 → 阶段 4 测试交付）全部完成**

## V2 迭代：批次 4（功能补全）完成（寇豆码，主理人抽查通过）

- 表单 3 字段：form.html + detail.html 编辑表单加进度%（number 0-100）/协同方/风险点（≤500 字），task_new/task_edit 经 _parse_form_extra_fields() 共享校验；**编辑时表单未携带 3 字段则保留原值不清零**（旧客户端兼容）
- 删除路由：POST /tasks/<id>/evidence/<eid>/delete、/tasks/<id>/blockers/<bid>/delete，仅 admin（@admin_required），AJAX 返回 JSON，普通 POST flash+重定向；models 新增 delete_evidence/delete_blocker；db_evidence_or_none 对齐 db_blocker_or_none 模式
- 时间线留痕（批次 3 遗漏项补齐）：添加证据/添加阻塞/标记解决/删除证据/删除阻塞 5 处操作写 progress_log（_log_timeline 封装，status_from/to=None，备注超 50 字截断）
- 抽屉/详情页删除入口：.delete-evidence-btn/.delete-blocker-btn[data-url]（main.js 事件委托，confirm+POST+Toast+drawer.reload），admin 视角才渲染
- 冒烟 57/57 通过；V1 回归 84/87（3 项失败均为批次 1-3 已知 V2 设计变更：/messages 301、登录错密码/停用账号 401，设计文档 §8 已列入阶段 4 用例更新计划）
- 视觉扫尾：全部页面逐页检查一致，仅修一处 .bl-actions flex-shrink 对证据项不生效的问题（内联补样式）
- 演示库已恢复基线（12 任务/0 证据/0 阻塞/0 冒烟残留）；smoke_batch4.py/server_v4.log 已删，端口已释放
- 下一步阶段 4：QA 严过关全量回归（87 项中 3 项用例按 §8 更新断言）+ 新增 25-30 项、同步 PRD/架构/test-cases-report 文档、PyInstaller 重新打包发行 zip

## V2 迭代：批次 3（抽屉交互）完成（寇豆码）

- 代码交付：7 个新路由（task_drawer/task_field/task_evidence_add/task_blocker_add/task_blocker_resolve/owner_drawer/task_remind）+ 3 个新片段模板（tasks/_drawer.html、tasks/_owner_drawer.html、messages/_drawer.html）+ list.html/detail.html 完全重写 + /messages 301 → /dashboard
- 冒烟测试 smoke_batch3.py 72/72 通过（字段白名单、权限矩阵、证据阻塞 CRUD、remind 落库等），验证后已删除脚本
- **关键 Bug 修复**：main.js 行点击拦截排除 `form` 导致批量表单包裹的任务表点行无法开抽屉 → 改排除 `label`，**不能排除 form**
- 真实浏览器验证全通过：任务行→抽屉（3 页签 8 可编辑字段）、行内编辑保存后抽屉自动 reload、矩阵负责人→Owner 抽屉+推送提醒、铃铛→消息抽屉、**消息条目点击→标记已读（铃铛 11→10）+打开关联任务抽屉**、阻塞页签添加+标记解决（resolved 类生效、按钮消失）
- 日志"未经命令的 GET /tasks/22/drawer + POST /tasks/22/field"确认为消息点击联动的正常交互（非异常）
- 演示库清理：删除测试任务 25/26/27（"01123"x2/"123"，批次2前遗留）+ 消息 66-73 + 日志 40-42 + 浏览器验证证据/阻塞 + task14/22 进度归零；恢复 12 条演示任务基线；备份 supervision.db.bak-v2batch3-dirty（迁移后脏状态，bak-v2batch2 是迁移前 schema）
- 环境清理：删截图/server_v2.log/smoke_batch3.py，关浏览器，停服务器；注意 sqlite3.connect() 会静默创建空 .db 文件，排查时误建 data/duiban.db 已删
- 下一步：批次 4（form.html 加进度%/风险点/协同方 3 字段 + evidence/blockers 完整 CRUD 收尾 + 视觉扫尾）→ 阶段 4（回归 87 项旧测试+新增、文档同步、PyInstaller 重新打包）

## V2 迭代：阶段 2 系统设计完成（2/4）

- 用户确认 Q1-Q9：Q1=B（只加进度%+风险点+协同方 3 字段），Q2-Q9 全选 A（完整证据/阻塞、抽屉+行内编辑、顶栏导航+消息抽屉、深色模式、仪表盘时间筛选、四级优先级保留、站内信推送提醒、闭环率排除已撤销）
- 产出设计文档：`docs/iteration-v2-design.md`，核心设计：
  - tasks 表 +3 列（progress_percent/risk_note/collaborators），新表 evidence（text/link/file）+ blockers（open/resolved），init_db 追加 _migrate_v2() 幂等迁移（PRAGMA table_info 检查列）
  - 新增路由：/tasks/<id>/drawer（HTML 片段）、/tasks/<id>/field（行内编辑 JSON 接口，字段白名单+can_edit_task）、evidence/blockers/remind 路由；/messages 页 301 → /dashboard，但 /messages/send 等全部保留
  - 前端：仍 Flask+Jinja2+原生 JS，不引框架/CDN；Lucide 图标做成 Jinja2 宏（约 25 个内联 SVG）；中文不内嵌 web 字体走系统字体（避免离线包膨胀 8MB+），仅 Inter 拉丁 woff2 本地化
  - models 新开 V2 统计函数（get_dashboard_stats_v2/get_today_focus/get_closure_matrix），不动旧函数保护测试
- 状态：等待用户确认设计文档后进入阶段 3 开发（批次 1 UI 底座）

## V2 迭代启动：差异分析（阶段 1/4）

- 用户提供新模板 `2026-08-28-01-44-37/artifacts/督办仓-离线版.html`（2.7MB 单页应用，Tailwind + Lucide + Inter 字体），作为 V2 迭代唯一视觉与功能基准
- 完成模板逆向分析：顶栏导航布局、6 统计卡（含闭环率）、今日督办焦点列表、行动闭环矩阵（按 Owner 汇总分页）、3 个右侧抽屉（Owner/详情 3 页签/消息）、进度弹窗、Toast、深色模式、登录页左右分栏
- 模板状态机与现有系统完全一致（5 态）；角色一致（admin/owner）
- 产出差异分析文档：`docs/iteration-v2-diff-analysis.md`
  - A 类 12 项视觉/布局差异（侧边栏→顶栏、emoji→Lucide、配色对齐等）
  - B 类 13 项模板有现有缺（闭环矩阵、焦点列表、证据/阻塞记录、行内编辑、进度%、推送提醒、时间范围筛选、深色模式等）
  - C 类 12 项现有有必须保留（批量操作、CSV 导出、用户管理、预警引擎、初始化向导等）
  - 提出 Q1-Q9 待确认问题（字段扩展范围、证据/阻塞表、抽屉交互、导航 IA、深色模式、筛选作用域、优先级体系、推送提醒实现、闭环率口径）
- 迭代方案：保留 Flask 多页架构不做前后端分离，4 个开发批次（UI 底座/仪表盘/抽屉/功能补全），表结构用 ALTER TABLE 平滑升级
- 状态：等待用户确认 Q1-Q9 后进入设计阶段

## 完成事项
完成督办系统 v1.0 最终交付包的系统性打包整理。

## 工作流程（SOP 标准流程）
团队：software-daiban-f75f
1. **通信自检**：4 位成员（许清楚/高见远/寇豆码/严过关）全部到岗确认
2. **Task #1 需求文档**（许清楚）：基于 PRD-draft-v1 整理为正式需求规格说明书，27 条功能需求（含 Given-When-Then 验收标准）+ 27 条非功能需求 + 业务流程说明，约 3.3 万字
3. **Task #2 设计文档**（高见远）：基于代码逆向整理 4 份设计文档（系统架构/数据库/接口/模块详细设计），含 Mermaid 图表
4. **Task #3 代码+打包**（寇豆码）：40 个源文件复制 + 注释审查通过 + PyInstaller onedir 打包（督办系统.exe，155 文件 25.75 MB）
5. **Task #4 测试报告**（严过关）：87 项测试全部通过（100% 通过率，零缺陷，耗时 205.93 秒）
6. **Task #5 汇总打包**（主理人）：生成总 README + 打包为 zip（13 MB）

## 交付物
- 交付目录：deliverables/督办系统-交付包/
- 压缩包：deliverables/督办系统-交付包.zip（13 MB，207 个文件）
- 目录结构：01_需求文档 / 02_设计文档 / 03_开发代码 / 04_测试 / 05_离线程序 / README.md

## 缺陷修复
- **bat 脚本中文乱码**：交付包内 4 个 .bat 文件（start.bat / 开启局域网访问.bat / 灌入演示数据.bat / 清除数据.bat）被工具以 UTF-8 编码写入，cmd.exe 用 GBK 解码导致乱码并报错
- 根因：Write 工具默认 UTF-8 编码，新建 .bat 时未指定 GBK
- 修复：Python 脚本统一转码为 GBK 编码，重新打包 zip
- 经验：所有 .bat/.cmd 脚本必须保存为 GBK 编码（已写入 MEMORY.md）
- 测试用例实际 87 项（比预估 73 项多 14 项，新增批量操作与 CSV 导出模块测试）
- 离线程序 25.75 MB，含 exe + _internal + 4 个 .bat 脚本
- 全程零缺陷，一次通过

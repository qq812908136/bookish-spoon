# 督办系统 — 文件整理方案与变更清单

> 生成时间：2026-08-31 ｜ 扫描范围：全库（排除 `.git` 内部对象，展开到三级）
> **本文档只做方案与清单，未移动、未删除、未修改任何文件。**
> 所有"删除"条目一律降级为「移入 `_archive/`」或「标注待确认」，等你拍板后才动手。

---

## 〇、执行摘要：三档力度，请选一档

| 档位 | 内容 | 涉及文件数 | 是否需改文件内容 | 是否触发交付包同步 | 推荐度 |
|---|---|---|---|---|---|
| **档位 1（推荐）** | 测试文件归位 `tests/`、根目录散落文档归入 `docs/迭代记录/`、构建脚本归位 `scripts/`、备份与产物隔离 | 约 12 项 | **否** | **否** | ⭐ 强烈推荐 |
| 档位 2 | 档位 1 + `docs/` 内部分组（需求/设计/测试/运维/迭代记录） | +8 项 | 否 | **是**（需同步 v3 的 01/02/04 并重打 zip） | 可选 |
| 档位 3 | 彻底分层：应用代码移入 `src/` | +10 项 | **是（7 处）** | 是 | ❌ 与你的约束冲突，仅列出代价供参考 |

**为什么推荐档位 1：** 项目存在一条隐藏硬约束——`config.py:29` 用 `BASE_DIR = dirname(abspath(__file__))` 推导数据库目录，`start.bat`/`build.bat`/`督办系统.spec` 全部用**相对于仓库根**的 `app.py`、`templates`、`static`。只要不动这几个文件，其余文件可以放心整理；一旦动，就必须改内容，直接违背"仅调整位置与命名"的前提。

---

## 一、现状：按功能模块归类的全量清单

### 1.1 应用运行时代码（**不可移动** — 导入根与路径推导依赖）

| 文件 | 体积 | 模块角色 | 不可移动的原因 |
|---|---|---|---|
| `app.py` | 17.4 KB | Flask 工厂 + 启动入口 | `start.bat` 调 `python app.py`；`.spec` 的 `Analysis(['app.py'])` |
| `config.py` | 3.8 KB | 全局配置 | `BASE_DIR=dirname(__file__)` → `data/` 路径；移动即换库 |
| `db.py` | 3.8 KB | 线程安全 SQLite 连接 | 被 models/state_machine/scheduler 同名导入 |
| `models.py` | 35.1 KB | 建表 + 全量 CRUD | 同上 |
| `state_machine.py` | 9.1 KB | 5 态状态机 | 同上 |
| `warning_engine.py` | 6.1 KB | 三层预警引擎 | 同上 |
| `scheduler.py` | 4.9 KB | 后台守护线程 | 同上 |
| `auth.py` | 6.5 KB | 认证 + RBAC | 同上 |
| `seed_demo_data.py` | 56.5 KB | 演示数据（9 用户/45 任务） | `app.py --seed-demo` 委托 |
| `routes/` | 71.7 KB / 8 文件 | 7 个 Blueprint | `app.py` 的 `from routes import ...` |
| `templates/` | 106 KB / 18 文件 | Jinja2 模板 | `config.TEMPLATE_DIR = BUNDLE_DIR/templates`；`.spec` datas |
| `static/` | 87 KB / 3 文件 | css/js/logo | `config.STATIC_DIR`；`.spec` datas |
| `data/supervision.db` | 144 KB | **正式数据库** | `config.DATABASE_PATH` 硬指向 |
| `start.bat` | 0.9 KB | 启动脚本 | 内含 `cd /d %~dp0` + `python app.py` |
| `开启局域网访问.bat` | 1.4 KB | 局域网启动辅助 | 同上，依赖当前目录 |
| `build.bat` | 0.9 KB | 打包脚本 | 内含 `--add-data "templates;templates"` |
| `督办系统.spec` | 0.9 KB | PyInstaller 配置 | `Analysis(['app.py'])` + datas 相对路径 |
| `requirements.txt` | 0.2 KB | 依赖清单 | `build.bat` 内 `pip install -r requirements.txt` |

### 1.2 测试（**可整体移动** — 内部用绝对路径）

| 文件 | 体积 | 说明 | 可移动性 |
|---|---|---|---|
| `test_suite.py` | 108.8 KB | 121 项 unittest | ✅ 第 34 行 `PROJECT_DIR` 是硬编码绝对路径，与自身位置无关 |
| `generate_test_report.py` | 10.5 KB | 跑测试生成 md/html 报告 | ✅ 仅 `import test_suite`（同目录），须与 test_suite 一起移动 |
| `test_data/test_supervision.db` | 100 KB | 测试库（运行产物，不入库） | ⚠️ **建议原地不动**：test_suite 绝对路径指向 `仓库根/test_data`，跟着移会在新位置重建空库 |

### 1.3 构建与交付脚本（**可移动** — 不 import 任何项目模块）

| 文件 | 体积 | 说明 | 可移动性 |
|---|---|---|---|
| `build_delivery_package.py` | 4.3 KB | v2→v3 汇编 + 打 zip | ✅ 只 import shutil/zipfile/datetime；内部 ROOT 为绝对路径 |

### 1.4 文档（**可自由移动** — 无任何代码引用）

| 文件 | 体积 | 现状位置问题 |
|---|---|---|
| `README.md` | 5.0 KB | 根目录 ✅ 合理（惯例保留） |
| `改动待办清单.md` | 154.7 KB | 154KB 台账放根目录，与 `docs/` 割裂 |
| `overview-058.md` | 1.0 KB | 根目录散落，编号不自解释 |
| `overview-059.md` | 1.7 KB | 同上 |
| `overview-交付包.md` | 3.3 KB | 同上 |
| `项目结构梳理报告-2026-08-31.md` | 19.1 KB | 上一轮产出，未归档 |
| `docs/PRD-draft-v1.md` | 46.6 KB | 需求类 |
| `docs/architecture-design.md` | 76.8 KB | 设计类 |
| `docs/iteration-v2-design.md` | 24.5 KB | 设计类（英文命名，风格不统一） |
| `docs/iteration-v2-diff-analysis.md` | 12.8 KB | 设计类（英文命名） |
| `docs/test-cases-report.html` | 76.7 KB | 测试类 |
| `docs/缺陷清单-2026-08-31.md` | 21.8 KB | 测试类 |
| `docs/Git使用说明.md` | 4.7 KB | 运维类 |
| `docs/上线前待办.md` | 5.8 KB | 运维类 |

### 1.5 临时 / 构建产物（**不入版本库，可隔离**）

| 条目 | 体积 | 性质 | 处置建议 |
|---|---|---|---|
| `build/` | 21 MB | PyInstaller **中间产物**（toc/pyz/pkg/localpycs） | 移 `_archive/` 或删除 |
| `dist/` | 27 MB | PyInstaller **输出**：exe + `_internal` | 与 v3 的 `05_离线程序` 内容重复，保留但可归入 `_archive/` |
| `督办系统-发行版.zip` | 13.6 MB / 169 条目 | 早期发行的**离线程序单包** | 移 `deliverables/_archive/` |
| `env_info.txt` | 139 B | 环境快照（Python 3.13.14，与现用 3.13.12 不符） | 移 `_archive/` |
| `data/*.db.bak-*` ×4 | 573 KB | 历史数据库备份 | 移 `data/backup/` |
| `__pycache__/` ×3 | — | Python 字节码缓存（根、routes、v2 副本内） | 可删，运行自动重建 |

### 1.6 交付包（`deliverables/`，123 MB）

| 条目 | 体积 | 状态 |
|---|---|---|
| `督办系统-交付包/`（v1） | 28 MB | 历史归档，已有 zip |
| `督办系统-交付包-v2/` | 29 MB | 历史归档，已有 zip |
| `督办系统-交付包-v3/` | 29 MB | **当前有效版本** |
| 3 个 zip（v1/v2/v3） | 40.6 MB | 归档价值 |

> 三个目录 + 三个 zip 共 **126.6 MB**，其中 v1/v2 目录（57 MB）已被各自的 zip 完整覆盖。

---

## 二、问题诊断（四类）

### 2.1 命名不规范

| # | 现状 | 问题 | 建议命名 |
|---|---|---|---|
| N1 | `docs/architecture-design.md` 与交付包 `督办系统-架构设计-最新.md` **字节数完全相同（76789）** | 同一份文档两个名字，且"最新"是典型的易腐命名 | 统一为 `督办系统-架构设计.md`，删除"-最新" |
| N2 | `督办系统-发行版.zip` | 无版本号、无日期；且"发行版/交付包"两个词并存 | `督办系统-离线程序-v3.zip` |
| N3 | `overview-058.md` / `overview-059.md` | 纯编号，不看内容不知道改了什么 | `overview-058-顶栏品牌区不可点击.md`、`overview-059-截止日期显示异常.md` |
| N4 | `docs/iteration-v2-*.md` | 中英混排，与同目录其余中文命名风格不一致 | `V2迭代设计.md` / `V2迭代差异分析.md`（注意：改名需同步交付包 02 副本） |
| N5 | `开启局域网访问.bat` | 名字承诺的功能与 `start.bat` 重叠，看不出是冗余件 | 合并进 `start.bat` 后淘汰，或改名 `start-局域网.bat` |
| N6 | `deliverables/督办系统-交付包-v2/04_测试/督办系统-测试用例报告-V2.html` | 用版本后缀而非日期，与同目录 `-2026-08-31` 命名法冲突 | `督办系统-测试用例报告-2026-08-28.html` |

### 2.2 放错目录

| # | 文件 | 现在 | 应在 | 理由 |
|---|---|---|---|---|
| P1 | `test_suite.py`（108 KB） | 根目录 | `tests/` | 交付包里已归入 `04_测试`，源码库却没有对应目录，两边不一致 |
| P2 | `generate_test_report.py` | 根目录 | `tests/` | 依附于 test_suite |
| P3 | `build_delivery_package.py` | 根目录 | `scripts/` | 构建工具，非应用代码 |
| P4 | `改动待办清单.md`、`overview-*.md`、结构梳理报告 | 根目录 | `docs/` | 文档类混入代码根 |
| P5 | `data/*.bak-*` ×4 | 正式数据目录 | `data/backup/` | 备份混入运行时数据目录 |
| P6 | `督办系统-发行版.zip` | 根目录 | `deliverables/_archive/` | 发行物不在发行目录 |
| P7 | `env_info.txt` | 根目录 | `_archive/` | 已过期快照 |

### 2.3 重复冗余

| # | 重复项 | 证据 | 处置 |
|---|---|---|---|
| R1 | 根目录 `督办系统-发行版.zip`（169 条目 / 00:21） vs `deliverables/督办系统-交付包-v3.zip`（229 条目 / 01:47） | 前者仅含离线程序，后者为五段式全包，前者时间更早 | 前者移 `_archive/` |
| R2 | `docs/architecture-design.md` == `02_设计文档/督办系统-架构设计-最新.md` | `wc -c` 均 76789 | 保留一份，另一份改符号链接或删除 |
| R3 | `docs/缺陷清单-2026-08-31.md` == `04_测试/缺陷清单-2026-08-31.md` | 均 21786 字节 | 源码库留 `docs/`，交付包打包时复制 |
| R4 | `docs/test-cases-report.html`（76667）vs `04_测试/督办系统-测试用例报告-2026-08-31.html`（28420） | 内容不同，但**名字完全不同**，看不出同源 | 统一命名规则 |
| R5 | `dist/督办系统/` vs `deliverables/v3/05_离线程序/督办系统/` | 同为 PyInstaller 输出，后者由前者手工同步而来 | 明确"dist 是中间态，05_离线程序 是交付态" |
| R6 | `build/` 21MB | PyInstaller 中间产物，一条命令可重建 | 移 `_archive/` 或删除 |
| R7 | v1（28M）+ v2（29M）交付包目录 | 已有同名 zip 完整归档 | 目录移 `deliverables/_archive/`，zip 保留 |
| R8 | `app.py:192` 的 `seed_demo_data()` | 源码内已注释"已废弃"的委托函数 | 属内容修改，本轮**不动**，仅登记 |

### 2.4 缺失（不在"整理"范围，但登记备查）

| # | 缺失 | 说明 |
|---|---|---|
| M1 | 根目录无 `清除数据.bat` / `灌入演示数据.bat` | **交付包 `03_开发代码` 里有，源码库没有** → 源码库与交付包不一致，建议从 v3 副本原样复制回根目录（纯复制，不改内容） |
| M2 | 无 `tests/` `scripts/` 目录 | 档位 1 补齐 |
| M3 | 无 `logs/` + logging 配置 | 需改 config.py，属内容修改，归入「上线前整改」 |
| M4 | 无 `.env` / `.env.example` | 同上（DEF-001，P0） |
| M5 | 无 `CHANGELOG.md` | 建议新建，合并 3 份 overview |
| M6 | `docs/` 缺接口设计/数据库设计/模块详细设计 | 只存在于交付包 02_设计文档 |

---

## 三、建议的目标目录结构

### 3.1 档位 1 落地后的形态（推荐）

```
督办系统/
├─ 【应用运行时 — 保持原样，一格不动】
│   ├─ app.py  config.py  db.py  models.py
│   ├─ auth.py  state_machine.py  warning_engine.py  scheduler.py  seed_demo_data.py
│   ├─ routes/                 8 文件
│   ├─ templates/              18 文件
│   ├─ static/                 css · js · img
│   ├─ data/
│   │   ├─ supervision.db      正式库（不动）
│   │   └─ backup/             ← 新增：4 份 .bak-* 移入
│   ├─ start.bat  开启局域网访问.bat  build.bat  督办系统.spec  requirements.txt
│   └─ README.md               惯例保留
│
├─ tests/                      ← 新增
│   ├─ test_suite.py           ← 移入
│   └─ generate_test_report.py ← 移入
│
├─ scripts/                    ← 新增
│   └─ build_delivery_package.py ← 移入
│
├─ docs/
│   ├─ 改动待办清单.md          ← 从根移入
│   ├─ 项目结构梳理报告-2026-08-31.md ← 从根移入
│   ├─ 项目整理方案与变更清单-2026-08-31.md ← 本文档
│   ├─ PRD-draft-v1.md
│   ├─ architecture-design.md
│   ├─ iteration-v2-design.md
│   ├─ iteration-v2-diff-analysis.md
│   ├─ test-cases-report.html
│   ├─ 缺陷清单-2026-08-31.md
│   ├─ Git使用说明.md
│   ├─ 上线前待办.md
│   └─ 迭代记录/                ← 新增
│       ├─ overview-058.md
│       ├─ overview-059.md
│       └─ overview-交付包.md
│
├─ test_data/                  不动（test_suite 绝对路径指向此处）
├─ deliverables/               v3 保留；v1/v2 目录移入 _archive/
├─ build/  dist/               保留（git 已忽略）
└─ _archive/                   ← 新增：所有"待你确认删除"的暂存区
    ├─ env_info.txt
    ├─ 督办系统-发行版.zip
    └─ （可选）build/ dist/ v1 v2 目录
```

### 3.2 档位 3 的终极形态（**需改 7 处内容，本轮不建议**）

```
督办系统/
├─ src/{app.py, config.py, db.py, models.py, ...}   ← 应用代码下沉
│   ├─ routes/  templates/  static/  data/
├─ tests/  scripts/  docs/  deliverables/
└─ *.bat / *.spec 留在根（作为启动器）
```
**必须同步修改的 7 处内容**（这是它与你"不改文件内容"约束的冲突点，列出供你判断）：

| # | 文件 | 必须改什么 |
|---|---|---|
| 1 | `config.py` | `BASE_DIR` 需改为上溯一层，否则数据库路径变成 `src/data/` |
| 2 | `督办系统.spec` | `Analysis(['src/app.py'])`、`pathex`、`datas` 三处路径 |
| 3 | `build.bat` | `app.py` → `src\app.py`；`--add-data` 两条路径 |
| 4 | `start.bat` | `python app.py` → `python src\app.py` |
| 5 | `test_suite.py` | `PROJECT_DIR` 绝对路径不变则无需改，但 `sys.path.insert` 需指向 `src` |
| 6 | `generate_test_report.py` | 输出目录参数 |
| 7 | `build_delivery_package.py` | 复制源清单从 `03_开发代码/` 改为 `src/` |

---

## 四、完整变更清单（档位 1，共 12 项）

> 全部为**移动/重命名**，无删除、无内容修改。执行前请逐条确认。

| # | 源路径 | 目标路径 | 动作 | 安全性依据 | 风险 |
|---|---|---|---|---|---|
| 1 | `test_suite.py` | `tests/test_suite.py` | 移动 | 第 34 行 `PROJECT_DIR` 硬编码绝对路径，与自身位置无关 | 无 |
| 2 | `generate_test_report.py` | `tests/generate_test_report.py` | 移动 | 仅 `import test_suite`，与 #1 同目录即成立 | 低：运行时须从仓库根执行 `python tests/generate_test_report.py <输出目录>`，显式传参绕开默认的 `04_测试` |
| 3 | `build_delivery_package.py` | `scripts/build_delivery_package.py` | 移动 | 不 import 任何项目模块；内部 ROOT 为绝对路径 | 无 |
| 4 | `改动待办清单.md` | `docs/改动待办清单.md` | 移动 | 纯人工台账，零代码引用 | 无 |
| 5 | `overview-058.md` | `docs/迭代记录/overview-058.md` | 移动 | 无引用 | 无 |
| 6 | `overview-059.md` | `docs/迭代记录/overview-059.md` | 移动 | 无引用 | 无 |
| 7 | `overview-交付包.md` | `docs/迭代记录/overview-交付包.md` | 移动 | 无引用 | 无 |
| 8 | `项目结构梳理报告-2026-08-31.md` | `docs/项目结构梳理报告-2026-08-31.md` | 移动 | 无引用 | 无 |
| 9 | `data/supervision.db.bak-seed-old` | `data/backup/` | 移动 ×1 | `config` 只拼 `DATABASE_PATH`，不枚举目录 | 无 |
| 10 | `data/supervision.db.bak-seed-v2` | `data/backup/` | 移动 | 同上 | 无 |
| 11 | `data/supervision.db.bak-v2batch2` | `data/backup/` | 移动 | 同上 | 无 |
| 12 | `data/supervision.db.bak-v2batch3-dirty` | `data/backup/` | 移动 | 同上 | 无 |

**外加 4 项待你确认的隔离动作（默认不执行）：**

| # | 源路径 | 建议目标 | 动作 | 说明 |
|---|---|---|---|---|
| 13 | `env_info.txt` | `_archive/env_info.txt` | 移动 | 139 B 过期快照（Python 3.13.14 vs 现用 3.13.12），已被 .gitignore |
| 14 | `督办系统-发行版.zip` | `_archive/督办系统-发行版.zip` | 移动 | 13.6 MB，与 v3.zip 重叠且更旧；已被 .gitignore |
| 15 | `deliverables/督办系统-交付包/` | `deliverables/_archive/督办系统-交付包/` | 移动 | 28 MB，已有同名 zip 归档 |
| 16 | `deliverables/督办系统-交付包-v2/` | `deliverables/_archive/督办系统-交付包-v2/` | 移动 | 29 MB，已有同名 zip 归档 |

**另建议 1 项复制（非移动，纯补缺）：**

| # | 源 | 目标 | 动作 | 说明 |
|---|---|---|---|---|
| 17 | `deliverables/v3/03_开发代码/清除数据.bat`<br>`deliverables/v3/03_开发代码/灌入演示数据.bat` | 仓库根目录同名文件 | **复制**（字节复制，不改内容） | 补齐源码库缺失的 2 个脚本，消除与交付包的结构差异 |

### 明确不动的白名单（动了就会跑不起来）

`app.py` `config.py` `db.py` `models.py` `auth.py` `state_machine.py` `warning_engine.py` `scheduler.py` `seed_demo_data.py` `routes/*` `templates/*` `static/*` `data/supervision.db` `data/.gitkeep` `test_data/` `start.bat` `开启局域网访问.bat` `build.bat` `督办系统.spec` `requirements.txt` `README.md` `docs/*`（档位 1 下不动）`.gitignore` `.gitattributes`

---

## 五、执行顺序与验证（确认后按此执行）

1. **备份先行**：`git status` 确认工作区干净（当前干净），移动后可用 `git status` 逐条复核。
2. 建 `tests/`、`scripts/`、`docs/迭代记录/`、`data/backup/`、`_archive/`。
3. 执行 #1–#12（移动）；如需隔离，执行 #13–#16。
4. **验证（三条命令，缺一不可）：**
   ```bash
   # ① 测试导入链未断（PROJECT_DIR 绝对路径，应照常跑通）
   python tests/test_suite.py TestStateMachine
   # ② Web 应用可启动（启动后浏览器开 127.0.0.1:5000，能登录即通过，随后关掉）
   python app.py
   # ③ 报告生成器能跑（从仓库根执行，显式传输出目录，绕开默认的 04_测试）
   python tests/generate_test_report.py docs/测试报告
   ```
5. `git status` 复核变更条数 = 预期，`git add -A` 后提交。
6. 因未触碰 `docs/` 与任何源码内容，**不触发交付包同步**（v3 zip 保持定稿状态）。

---

## 六、需要你拍板的 5 个问题

1. **档位选哪个？** 推荐档位 1（零风险、12 项、不触发交付包同步）。
2. **#13–#16 的隔离动作做不做？** 做了可回收约 **84 MB** 的视觉噪音（`_archive/` 会被我加进 `.gitignore`——注意这需要改 `.gitignore` 一行，是否允许？）。
3. **#17 复制两个 bat 回根目录做不做？** 做了能消除源码库与交付包的结构差异，但是"新增文件"而非整理。
4. **档位 2（`docs/` 分组）要不要一起做？** 做了必须同步 v3 的 01/02/04 并重打 zip（v3.0 已打 tag，等于要刷新已发行的交付包）。
5. **命名规范 N1–N6 是否采纳？** 其中 N1/N4 会连带影响交付包副本命名，同样触发同步。

---

## 七、一句话结论

真正值得动的是**测试、脚本、散落文档、备份、过期产物**这五类（合计约 12–16 项），它们与运行时代码零耦合，移动后 `python app.py`、`python tests/test_suite.py`、`pyinstaller 督办系统.spec` 三条主链路**都不受影响**；而应用代码与 `templates`/`static`/`data` 因路径推导和相对引用被焊死在仓库根，除非你同意修改 7 处文件内容，否则不应移动。
